# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database URLs - Optimized for Serverless
# Get from: Supabase Dashboard > Project Settings > Database > Connection String

# 1. DATABASE_URL - Transaction pooler (port 6543)
#    Used by: Running app (API routes, serverless functions)
#    Why: Optimized for high-concurrency serverless workloads
DATABASE_URL="postgresql://postgres.your-project:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"

# 2. MIGRATION_URL - Session pooler (port 5432)
#    Used by: Migrations, seeding, database setup scripts
#    Why: Supports prepared statements required by Prisma migrations
MIGRATION_URL="postgresql://postgres.your-project:password@aws-0-region.pooler.supabase.com:5432/postgres"

# 3. DIRECT_URL - Direct connection (not used, kept for reference)
#    Note: Often blocked by firewall, not needed for this setup
DIRECT_URL="postgresql://postgres:password@db.your-project.supabase.co:5432/postgres"

# Upstash Redis (optional - for rate limiting)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_URL=your-redis-rest-url
UPSTASH_REDIS_TOKEN=your-redis-rest-token

# Node Environment
NODE_ENV=development
